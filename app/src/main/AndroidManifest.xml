<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tap_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AutoCom"></activity>
        <activity android:name=".SchoolAttendance" />
        <activity android:name=".Whatsapp" />
        <activity android:name=".MainHome" />
        <activity android:name=".QuestionsVolunteer" />
        <activity android:name=".Tap_Home" />
        <activity android:name=".SchoolAdminHome" />
        <activity android:name=".TapAdminHome" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>